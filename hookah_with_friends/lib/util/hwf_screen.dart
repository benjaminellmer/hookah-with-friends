import "package:flutter/material.dart";

class HWFScreen extends StatelessWidget {
  const HWFScreen({super.key, required this.appBar, required this.body});

  final AppBar appBar;
  final Widget body;

  @override
  Widget build(BuildContext context) {
    return body;
  }
}
